@inject IStringLocalizer<Resource> lang
@if (is_visible)
{
    <label class="uk-label uk-label-danger text-center">@lang["Expired"]</label>
}

@code{
    [Parameter] public bool is_visible { get; set; }
}