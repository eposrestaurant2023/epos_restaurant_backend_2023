@inject IStringLocalizer<Resource> lang
@if (is_visible)
{

    <span class="uk-label unpaid" style="background:red">@lang["Unpaid"]</span>
}

@code{
    [Parameter] public bool is_visible { get; set; }
}