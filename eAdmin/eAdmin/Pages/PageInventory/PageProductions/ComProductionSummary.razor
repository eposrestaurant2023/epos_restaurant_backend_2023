@inject IStringLocalizer<Resource> lang
<Grid ClassName="uk-grid-small">
    <div class="uk-width-2-3@s">
        <MudTabs PanelClass="pa-6">
            <MudTabPanel Text="@lang["Note"]">
                <TextInput type="textarea" @bind-text="@model.note" placeholder="@lang["Note"]" />
            </MudTabPanel>
            <MudTabPanel Text="@lang["Term & Conditions"]">
                <TextInput type="textarea" @bind-text="@model.term_conditions" placeholder="@lang["Term & Conditions"]" />
            </MudTabPanel>
        </MudTabs>
    </div>
    <div class="uk-width-1-3@s">
        <div class="wrp-sub-total uk-form-horizontal">
            <div class="uk-width-1-1">
                <div class="uk-grid-small" uk-grid>
                    <div class="uk-width-expand" uk-leader>@lang["Total Amount"]</div>
                    <div>@model.total_amount.ToString(gv.main_currency_format)</div>
                </div>
            </div>
        </div>
    </div>
</Grid>
@code{

    [CascadingParameter] public GlobalVariableModel gv { get; set; }
    [Parameter] public ProductionModel model { get; set; }
    [Parameter] public EventCallback<ProductionModel> modelChanged { get; set; }

}
