

@code {
    [Parameter] public int product_id { get; set; }
    List<ProductIngredientRelatedModel> models = new List<ProductIngredientRelatedModel>();

   @*ProductIngredientRelated?$filter=product_id eq 1555&$expand=product*@
}
