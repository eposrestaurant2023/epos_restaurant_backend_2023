@if (is_visible)
{
    if (is_loading)
    {
        <Spinner />
    }
    else
    {


        if (roles == "")
        {

             <MudChip  Label="true" Color="MudBlazor.Color.Error" Icon="@IconName" Size="Size.Small">InAtive</MudChip>
        }
        else
        {
            <AuthorizeView Roles="@roles">
                <Authorized>
                       <MudChip  Label="true" Color="MudBlazor.Color.Error" Icon="@IconName" Size="Size.Small" OnClick="@OnClick">InAtive</MudChip>
                </Authorized>
                <NotAuthorized>
                     <MudChip  Label="true" Color="MudBlazor.Color.Error" Icon="@IconName" Size="Size.Small" OnClick="@OnClick">InAtive</MudChip>
                </NotAuthorized>
            </AuthorizeView>
        }
    }

}


@code
{
    [Parameter] public bool is_visible { get; set; } = true;
    [Parameter] public bool is_loading { get; set; }
    [Parameter] public string roles { get; set; } = "";
    [Parameter] public string ClassName { get; set; } = "";
    [Parameter] public string IconName { get; set; } = "";
    [Parameter] public string text { get; set; } = "Inactive";

    [Parameter] public EventCallback<MouseEventArgs> OnClick { get; set; }

}
