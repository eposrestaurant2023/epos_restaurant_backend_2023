@if (is_visible)

{
    <MudChip  Label="true" Color="MudBlazor.Color.Error"  Size="Size.Small">Duration (@count)</MudChip>
}

@code {
    [Parameter] public bool is_visible { get; set; } = true;
    [Parameter] public int count { get; set; } = 0;
}
