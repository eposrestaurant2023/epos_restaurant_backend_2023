@if (is_loading)
{
    <MudButton StartIcon="@Icons.Material.Filled.Save" Variant="Variant.Filled" Color="MudBlazor.Color.Success">
    @if (@ChildContent == null)
    {
        @("Save")
    }else{
     @ChildContent
    }

    <Spinner/>
    
</MudButton>
}else {
    <MudButton StartIcon="@Icons.Material.Filled.Save" OnClick="@OnClick" Variant="Variant.Filled" Color="MudBlazor.Color.Success" ButtonType="@button_type">
    @if (@ChildContent == null)
    {
        @("Save")
    }else{
     @ChildContent
    }
</MudButton>
}




@code {
        [Parameter] public RenderFragment ChildContent { get; set; }
        [Parameter] public EventCallback<MouseEventArgs> OnClick { get; set; }
        [Parameter] public bool is_loading{ get; set; }
        [Parameter] public ButtonType button_type { get; set; } = ButtonType.Submit;
        
}
