@if (visible)
{
    if (roles == "")
    {
        <MudChip  Label="true" Color="MudBlazor.Color.Primary"  Size="Size.Small">Full License</MudChip>
    }
    else
    {
        <AuthorizeView Roles="@roles">
            <Authorized>
                <MudChip  Label="true" Color="MudBlazor.Color.Primary" Size="Size.Small">Full License</MudChip>
            </Authorized>
            <NotAuthorized>
                    <MudChip  Label="true" Color="MudBlazor.Color.Primary" Size="Size.Small">Full License</MudChip>
            </NotAuthorized>
        </AuthorizeView>
    }
}

@code{
    [Parameter] public string roles { get; set; }
    [Parameter] public bool visible { get; set; }
    
}